{{ form_start(form) }}
<div class="card-body">
    <div class="row">
        <div class="col-12">
            {{ form_errors(form) }}
            {{ form_widget(form) }}
        </div>
    </div>
</div>
<div class="card-footer">
    <button class="btn btn-primary">{% trans %}Update{% endtrans %}</button>
</div>
{{ form_end(form) }}